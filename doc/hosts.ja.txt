ホスト指定
~~~~~~~~~~

ホスト名は --hosts オプションにより、または環境変数 HOSTS により
コンマ区切りの文字列として指定できます。

  $ ./cin @role --hosts "192.168.1.2, 192.168.120.1" task
  $ HOSTS="10.10.2.1,10.10.2.2" ./cin @role task

両方が指定されたときは --hosts が優先されます。どちらも指定されない
ときは role によって決まるホスト(群)が使われます。

コンマの前後にはスペースを挿入しても構いません。
